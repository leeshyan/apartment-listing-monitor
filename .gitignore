# Personal config — contains your email, phone, neighborhood, and listing URLs
config.yaml
config.yml

# State files — machine-generated, can be large
seen-listings.json
daily-log.json

# Backup state files
*.bak

# Packaged plugin file (distributed via GitHub Releases, not committed to repo)
*.plugin

# macOS
.DS_Store

# Editor
.vscode/
.idea/
*.swp

# Cowork runtime
.claude/
